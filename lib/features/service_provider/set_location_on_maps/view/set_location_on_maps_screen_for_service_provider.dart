import 'package:evento/core/responsive/responsive.dart';
import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class SetLocationOnMapsForServerProvider extends StatelessWidget {
  const SetLocationOnMapsForServerProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SizedBox(
        width: screenWidth,
        height: screenHeight,
      ),
    );
  }
}
